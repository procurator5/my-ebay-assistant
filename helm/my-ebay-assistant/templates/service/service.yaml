apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-ebay-assistant.fullname" . }}
  labels:
    {{- include "my-ebay-assistant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-ebay-assistant.selectorLabels" . | nindent 4 }}
